const TEST = {
  "situations": [
    {
      "points": 5,
      "title": "Situația 1: Refuz coordonat la revenirea în celulă",
      "scenario": "După plimbare, trei deținuți refuză să revină în celule. Unul cere să vorbească urgent cu psihologul, iar altul încearcă să îi convingă și pe ceilalți să nu se supună.",
      "question": "Cum gestionezi situația, fără să escaladezi conflictul?"
    },
    {
      "points": 5,
      "title": "Situația 2: Obiect interzis într-o celulă comună",
      "scenario": "În timpul unei percheziții într-o celulă cu patru deținuți, găsești o cartelă SIM ascunsă într-un loc greu accesibil. Niciun deținut nu recunoaște obiectul.",
      "question": "Ce pași urmezi pentru ridicare, raportare și identificarea responsabilului?"
    },
    {
      "points": 5,
      "title": "Situația 3: Conflict verbal care poate deveni fizic",
      "scenario": "Doi deținuți se jignesc repetat în zona comună. Unul se apropie amenințător, iar ceilalți deținuți încep să se adune în jurul lor.",
      "question": "Cum intervii pentru a preveni un incident mai grav?"
    },
    {
      "points": 5,
      "title": "Situația 4: Criză psihologică",
      "scenario": "Un deținut îți spune că nu mai poate face față situației și pare într-o stare psihologică instabilă. Refuză inițial să vorbească cu personalul medical.",
      "question": "Care sunt măsurile corecte și urgente pe care trebuie să le iei?"
    },
    {
      "points": 5,
      "title": "Situația 5: Pachet suspect la sectorul vizite",
      "scenario": "O persoană aduce un pachet pentru un deținut. În timpul verificării, persoana devine agitată și încearcă să grăbească procesul, spunând că nu are timp.",
      "question": "Cum procedezi pentru verificare, documentare și informarea superiorilor?"
    },
    {
      "points": 5,
      "title": "Situația 6: Refuzul percheziției și acuzații împotriva personalului",
      "scenario": "Un deținut refuză percheziția corporală și susține că agentul îl discriminează. În același timp, devine recalcitrant și ridică tonul.",
      "question": "Cum acționezi ca să respecți procedura și să eviți abuzul?"
    },
    {
      "points": 5,
      "title": "Situația 7: Neconcordanță la apel",
      "scenario": "La apelul de dimineață, numărul deținuților nu corespunde cu evidența. Un coleg spune că este posibil să fie o eroare de sector, dar există și o zonă fără vizibilitate completă pe camere.",
      "question": "Care este ordinea corectă a verificărilor și a raportării?"
    }
  ],
  "quiz": [
    {
      "points": 3,
      "question": "Într-o situație tensionată, care este prima reacție corectă a unui agent de penitenciar?",
      "options": {
        "A": "Aplică imediat sancțiuni pentru a impune frica",
        "B": "Încearcă de-escaladarea verbală și respectă procedura",
        "C": "Lasă situația să se rezolve singură",
        "D": "Amenință deținutul cu izolarea fără raport"
      }
    },
    {
      "points": 3,
      "question": "Ce este cel mai important când se găsește un obiect interzis?",
      "options": {
        "A": "Să fie aruncat rapid pentru a închide incidentul",
        "B": "Să fie fotografiat pentru social media",
        "C": "Să fie ridicat, securizat, raportat și predat conform procedurii",
        "D": "Să fie păstrat de agent până la finalul turei"
      }
    },
    {
      "points": 3,
      "question": "Când poate fi folosită forța în mod justificat?",
      "options": {
        "A": "Când deținutul răspunde obraznic",
        "B": "Când este necesar pentru siguranță, ordine sau autoapărare, proporțional și legal",
        "C": "Când agentul vrea să transmită autoritate",
        "D": "Ori de câte ori un ordin nu este executat instant"
      }
    },
    {
      "points": 3,
      "question": "Ce faci dacă un deținut cere ajutor medical, dar situația pare și un posibil pretext?",
      "options": {
        "A": "Îl ignori până recunoaște că minte",
        "B": "Îl pedepsești pentru încercare de manipulare",
        "C": "Menții siguranța, anunți personalul competent și documentezi situația",
        "D": "Îl lași nesupravegheat ca să se calmeze"
      }
    },
    {
      "points": 3,
      "question": "Ce trebuie să conțină un raport de incident bun?",
      "options": {
        "A": "Doar concluzia agentului",
        "B": "Fapte observabile, oră, loc, persoane implicate, măsuri luate și martori",
        "C": "Păreri personale despre caracterul deținutului",
        "D": "Doar numele deținutului sancționat"
      }
    },
    {
      "points": 3,
      "question": "Ce faci dacă un coleg propune să se sară peste o procedură pentru că „nu avem timp”?",
      "options": {
        "A": "Accepți, dacă este coleg cu vechime",
        "B": "Respecți procedura și informezi superiorul dacă abaterea poate afecta siguranța",
        "C": "Ignori complet situația",
        "D": "Îl lași pe coleg să semneze în locul tău"
      }
    },
    {
      "points": 3,
      "question": "Ce variantă descrie cel mai bine principiul proporționalității?",
      "options": {
        "A": "Răspunsul agentului trebuie să fie mai dur decât acțiunea deținutului",
        "B": "Măsura aleasă trebuie să fie necesară și adecvată riscului real",
        "C": "Orice refuz permite intervenție maximă",
        "D": "Proporționalitatea se aplică doar civililor"
      }
    },
    {
      "points": 3,
      "question": "În RP, ce este greșit la acțiunea: „/me îl pune instant la pământ și îl încătușează fără șanse de reacție”?",
      "options": {
        "A": "Este un exemplu de șanse egale",
        "B": "Este Power Gaming, deoarece impune rezultatul fără reacția celuilalt",
        "C": "Este permis mereu pentru facțiunile legale",
        "D": "Este doar o descriere cosmetică"
      }
    },
    {
      "points": 3,
      "question": "Ce faci dacă observi o posibilă tentativă de evadare, dar nu ai confirmare clară?",
      "options": {
        "A": "Declari imediat public că deținutul a evadat",
        "B": "Verifici rapid evidențele, anunți superiorii și aplici procedura internă de alarmare dacă suspiciunea persistă",
        "C": "Aștepți finalul turei pentru a raporta",
        "D": "Întrebi ceilalți deținuți dacă au văzut ceva și te bazezi doar pe ei"
      }
    },
    {
      "points": 3,
      "question": "Care este comportamentul corect al unui agent într-o situație cu presiune publică la vizite?",
      "options": {
        "A": "Grăbește controlul ca să evite discuțiile",
        "B": "Respectă procedura, păstrează calmul și documentează orice neregulă",
        "C": "Acceptă pachetul fără verificare dacă vizitatorul insistă",
        "D": "Confiscă tot fără documente"
      }
    }
  ],
  "rules": [
    {
      "points": 2,
      "title": "Explică diferența dintre IC și OOC și de ce este importantă în Roleplay."
    },
    {
      "points": 4,
      "title": "Ce înseamnă Power Gaming? Dă un exemplu greșit și rescrie-l corect, cu șanse egale."
    },
    {
      "points": 4,
      "title": "Explică No-Fear și Cop-Fear. Dă câte un exemplu pentru fiecare, în context de penitenciar sau facțiune legală."
    },
    {
      "points": 3,
      "title": "Ce înseamnă Cop-Bait și Provoking? Explică diferența dintre ele."
    },
    {
      "points": 3,
      "title": "Explică diferența dintre VDM și RDM și dă câte un exemplu."
    },
    {
      "points": 3,
      "title": "Ce reguli se aplică atunci când un jucător este inconștient? Ce nu are voie să facă?"
    },
    {
      "points": 5,
      "title": "În ce condiții se poate aplica Character Kill? Dă două exemple valide RP."
    },
    {
      "points": 4,
      "title": "Este permisă jefuirea unui membru al unei facțiuni legale aflat în timpul serviciului? Explică răspunsul."
    },
    {
      "points": 3,
      "title": "Care sunt zonele publice, retrase și rău famate? Dă exemple și spune de ce contează în RP."
    },
    {
      "points": 4,
      "title": "Pe drumuri neasfaltate, ce limite de viteză se aplică vehiculelor offroad și în ce caz se poate depăși limita normală?"
    }
  ]
};

function el(tag, className, text) {
  const node = document.createElement(tag);
  if (className) node.className = className;
  if (text !== undefined) node.textContent = text;
  return node;
}

function renderSituations() {
  const root = document.getElementById("situations");
  TEST.situations.forEach((item, index) => {
    const box = el("article", "question");
    const top = el("div", "question-top");
    top.append(el("h3", "", `${index + 1}. ${item.title}`), el("span", "points", `${item.points}p`));
    const scenario = el("p", "scenario", `Situație: ${item.scenario}`);
    const q = el("p", "", `Întrebare: ${item.question}`);
    const label = document.createElement("label");
    label.textContent = "Răspunsul tău";
    const textarea = document.createElement("textarea");
    textarea.id = `situation-${index}`;
    textarea.placeholder = "Scrie aici răspunsul complet...";
    label.appendChild(textarea);
    box.append(top, scenario, q, label);
    root.appendChild(box);
  });
}

function renderQuiz() {
  const root = document.getElementById("quiz");
  TEST.quiz.forEach((item, index) => {
    const box = el("article", "question");
    const top = el("div", "question-top");
    top.append(el("h3", "", `${index + 1}. ${item.question}`), el("span", "points", `${item.points}p`));
    const options = el("div", "options");
    Object.entries(item.options).forEach(([key, value]) => {
      const label = el("label", "option");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = `quiz-${index}`;
      input.value = key;
      label.append(input, document.createTextNode(`${key}. ${value}`));
      options.appendChild(label);
    });
    box.append(top, options);
    root.appendChild(box);
  });
}

function renderRules() {
  const root = document.getElementById("rules");
  TEST.rules.forEach((item, index) => {
    const box = el("article", "question");
    const top = el("div", "question-top");
    top.append(el("h3", "", `${index + 1}. ${item.title}`), el("span", "points", `${item.points}p`));
    const label = document.createElement("label");
    label.textContent = "Răspunsul tău";
    const textarea = document.createElement("textarea");
    textarea.id = `rule-${index}`;
    textarea.placeholder = "Scrie aici răspunsul...";
    label.appendChild(textarea);
    box.append(top, label);
    root.appendChild(box);
  });
}

function encodePayload(obj) {
  const json = JSON.stringify(obj);
  return btoa(unescape(encodeURIComponent(json)));
}

function collectAnswers() {
  return {
    version: "penitenciar-v2",
    submittedAt: new Date().toISOString(),
    candidateName: document.getElementById("candidateName").value.trim(),
    discordName: document.getElementById("discordName").value.trim(),
    situations: TEST.situations.map((item, index) => ({
      title: item.title,
      points: item.points,
      answer: document.getElementById(`situation-${index}`).value.trim()
    })),
    quiz: TEST.quiz.map((item, index) => {
      const selected = document.querySelector(`input[name="quiz-${index}"]:checked`);
      return {
        question: item.question,
        points: item.points,
        selected: selected ? selected.value : ""
      };
    }),
    rules: TEST.rules.map((item, index) => ({
      title: item.title,
      points: item.points,
      answer: document.getElementById(`rule-${index}`).value.trim()
    }))
  };
}

function buildEvaluatorUrl(payload) {
  const encoded = encodePayload(payload);
  const current = new URL(window.location.href);
  let evaluatorUrl;

  if (current.pathname.includes("/candidat/")) {
    evaluatorUrl = new URL(current.href.replace("/candidat/", "/evaluator/"));
  } else {
    evaluatorUrl = new URL("../evaluator/index.html", current.href);
  }

  evaluatorUrl.hash = `payload=${encoded}`;
  return { url: evaluatorUrl.toString(), code: encoded };
}

function generateLink() {
  const answers = collectAnswers();

  if (!answers.candidateName || !answers.discordName) {
    document.getElementById("statusMessage").textContent = "Completează numele și Discordul înainte să generezi linkul.";
    return;
  }

  const emptyOpen = answers.situations.filter(x => !x.answer).length + answers.rules.filter(x => !x.answer).length;
  const emptyQuiz = answers.quiz.filter(x => !x.selected).length;

  const { url, code } = buildEvaluatorUrl(answers);
  document.getElementById("shareLink").textContent = url;
  document.getElementById("payloadCode").value = code;
  document.getElementById("shareBox").classList.add("show");
  document.getElementById("statusMessage").textContent =
    emptyOpen || emptyQuiz
      ? `Link generat, dar ai ${emptyOpen + emptyQuiz} răspunsuri necompletate.`
      : "Link generat. Trimite-l evaluatorului.";

  localStorage.setItem("penitenciarV2Answers", JSON.stringify(answers));
}

async function copyText(text) {
  try {
    await navigator.clipboard.writeText(text);
    document.getElementById("statusMessage").textContent = "Copiat.";
  } catch {
    document.getElementById("statusMessage").textContent = "Nu s-a putut copia automat. Selectează și copiază manual.";
  }
}

function saveLocal() {
  const answers = collectAnswers();
  localStorage.setItem("penitenciarV2Answers", JSON.stringify(answers));
  document.getElementById("statusMessage").textContent = "Răspunsurile au fost salvate local în browser.";
}

function loadLocal() {
  const saved = localStorage.getItem("penitenciarV2Answers");
  if (!saved) return;
  try {
    const answers = JSON.parse(saved);
    document.getElementById("candidateName").value = answers.candidateName || "";
    document.getElementById("discordName").value = answers.discordName || "";
    answers.situations?.forEach((x, i) => {
      const f = document.getElementById(`situation-${i}`);
      if (f) f.value = x.answer || "";
    });
    answers.rules?.forEach((x, i) => {
      const f = document.getElementById(`rule-${i}`);
      if (f) f.value = x.answer || "";
    });
    answers.quiz?.forEach((x, i) => {
      if (x.selected) {
        const input = document.querySelector(`input[name="quiz-${i}"][value="${x.selected}"]`);
        if (input) input.checked = true;
      }
    });
  } catch {}
}

document.addEventListener("DOMContentLoaded", () => {
  renderSituations();
  renderQuiz();
  renderRules();
  loadLocal();

  document.getElementById("generateBtn").addEventListener("click", generateLink);
  document.getElementById("saveBtn").addEventListener("click", saveLocal);
  document.getElementById("printBtn").addEventListener("click", () => window.print());
  document.getElementById("copyLinkBtn").addEventListener("click", () => copyText(document.getElementById("shareLink").textContent));
  document.getElementById("copyCodeBtn").addEventListener("click", () => copyText(document.getElementById("payloadCode").value));
});
